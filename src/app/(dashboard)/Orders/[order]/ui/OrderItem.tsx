import { Tables } from "@/types/database.types";
import React from "react";

export default function OrderItem({
  item,
}: {
  item: Tables<"order_products">;
}) {
  return (
    <div>
      <div></div>
    </div>
  );
}
